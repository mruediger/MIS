TODO:
    versionierung (per index datei?)
    cleanup datastore
    unionfs
    acl

Cleanup Datastore:
    liste von hashes aus manifesten extrahieren
    manifeste oder liste von server anfordern

Refactoring:
    entschlacken von manifest klasse
    with fuer datei operationen
    bla or 'default' anstatt hasattr()
    deprecated decorator implementieren
    pychecker

Performance Test:
    dateisystem caching optimieren
    einlesen von manifests langsam (pickle?)

Mogliche Fehler:
    Dateisystem locking anschauen
    wie funktioniert SHA-1
    unionfs mit whiteouts im basis layer testen (muessen bleiben)
    
Filtern:
    - scripte auf readonly + unionfs und dann erst importieren

Exportieren:
    bashscript + schreiben in sparse image
    - xen
    - vmware
    - virtualbox

Use Cases:
    update + filter layer ohne basis layer

Packaging:
    pychecker
