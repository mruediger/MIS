Performance Test:
    parallels hashing pro/contra
    dateisystem caching optimieren
    einlesen von manifests langsam

Mogliche Fehler:
    Dateisystem locking anschauen
    wie funktioniert SHA-1

    unionfs mit whiteouts im basis layer testen (muessen bleiben)
    
Refactoring:
    entschlacken von manifest klasse

Filtern:
    - scripte auf readonly + unionfs und dann erst importieren


Exportieren:
    bashscript + schreiben in sparse image
    - xen
    - vmware
    - virtualbox

Fragen:
    versionierung in manifest dateien
    use cases (cli samples)
    speichern wo und wie und warum

Muse Have:
    manifest erzeugung und datastore speicherung trennen
        ( create_datastore manifest.xml source dest )
    unionfs/aufs behandlung mit delete_nodes im manifest
    export von delete_nodes als aufs/unionfs
    editieren von images mit delete nodes

    netzwerktool zum updaten von images uebers netz


Use Cases:
    update + filter layer ohne basis layer

Nice to have:
    cleanup datastore
    ACL
    ctime

